/// Public API for the `leng_responsive_helper` package.
///
/// This file exists so the package's top-level library name matches the
/// package name expected by pub tooling (exporting the real implementation).
library;

export 'responsive_helper.dart';
